# Dependencies
node_modules/

# Secrets — NEVER commit this
.env

# Build output (regenerated by `npm run css`)
public/css/app.css

# Logs / OS / editor cruft
npm-debug.log*
*.log
.DS_Store
Thumbs.db
.vscode/

# Local visual previews rendered with fake data. Never deployed.
public/_preview/

# Database backups hold every patient record. The repository is PUBLIC:
# these must never be committed.
backups/
*.backup.json
